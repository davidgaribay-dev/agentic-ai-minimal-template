# E2E Test Environment Variables

# Base URL for the frontend application
# Default: http://localhost:5173 (local dev server)
E2E_BASE_URL=http://localhost:5173

# Set to any value to enable CI mode
# - Disables webServer auto-start (expects app to be running)
# - Enables retries
# - Uses single worker
# CI=true
